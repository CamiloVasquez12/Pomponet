@model IEnumerable<PomponetWebsite.Models.Fungicide_X_Pompon_Part>

@{
    ViewData["Title"] = "Index";
}

<h1>List Fungicide_X_Pompon_Part</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<form asp-controller="Fungicide_X_Pompon_Part" asp-action="Index" method="get">
    <p>
        Buscar: <input type="text" name="buscar" class="form-text" />
        <input type="submit" value="Buscar" class="btn btn-sm btn-dark" />
        @if (!string.IsNullOrEmpty(ViewData["Error"] as string))
        {
            <span class="text-danger">@ViewData["Error"]</span>
        }
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-filtro="@ViewData["FiltroId_Pompon_Part"]">
                    @Html.DisplayNameFor(model => model.Id_Pompon_Part)
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-filtro="@ViewData["FiltroId_Fungicide"]">
                    @Html.DisplayNameFor(model => model.Id_Fungicide)
                </a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deleted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Pompon_Part)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Fungicide)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deleted)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Id_Fungicide_X_Pompon_Part">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.Id_Fungicide_X_Pompon_Part">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id_Fungicide_X_Pompon_Part">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

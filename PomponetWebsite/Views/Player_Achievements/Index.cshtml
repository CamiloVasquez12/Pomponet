@model IEnumerable<PomponetWebsite.Models.Player_Achievements>

@{
    ViewData["Title"] = "Index";
}

<h1>List Player_Achievements</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<form asp-controller="Player_Achievements" asp-action="Index" method="get">
    <p>
        Buscar: <input type="text" name="buscar" class="form-text" />
        <input type="submit" value="Buscar" class="btn btn-sm btn-dark" />
        @if (!string.IsNullOrEmpty(ViewData["Error"] as string))
        {
            <span class="text-danger">@ViewData["Error"]</span>
        }
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-filtro="@ViewData["FiltroId_Achievement"]">
                    @Html.DisplayNameFor(model => model.Id_Achievement)
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-filtro="@ViewData["FiltroLogros_Totales"]">
                    @Html.DisplayNameFor(model => model.Logros_Totales)
                </a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_Player)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Deleted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Achievement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Logros_Totales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Player)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deleted)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Id_Player_Achievement">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.Id_Player_Achievement">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id_Player_Achievement">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
